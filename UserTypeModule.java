package medicalstore.masterdata;
import javax.swing.*;

public class UserTypeModule extends JFrame {
    public UserTypeModule() {
        setTitle("User Type Module");
        setSize(400, 300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(DISPOSE_ON_CLOSE);
        add(new JLabel("User Type Module Window", JLabel.CENTER));
    }
}
